{
  "name": "nodejs-crash-2024",
  "version": "1.0.0",
  "description": "Node.js crash course code",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "nodemon --env-file=.env server2.js",
    "stop": "echo \"Stopping the node application (to be defined)\" && exit 1",
    "build": "node --env-file=.env server2.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Javier Pascual",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
